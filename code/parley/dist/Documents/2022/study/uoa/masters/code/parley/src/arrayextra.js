class NumArray extends Array {
    x;
    constructor(x) {
        super(...x);
    }
}
class StringArray extends Array {
    x;
    constructor(x) {
        super(...x);
    }
}
class BoolArray extends Array {
    x;
    constructor(x) {
        super(...x);
    }
}
export { NumArray, StringArray, BoolArray };
